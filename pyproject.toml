[project]
name = "atlas"
version = "0.4.0"
description = "ATLAS - Autonomous operations agent for Google All Things Agentic Hackathon 2026"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
    "ignore::DeprecationWarning",
]
